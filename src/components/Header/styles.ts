import styled from 'styled-components'
import { Panel } from '../Panel'

export const HeaderRoot = styled(Panel)`
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 8px;
  margin-bottom: 8px;
`

export const Toolbar = styled.div`
  display: flex;
`

export const SettingsButton = styled.div``
